// return any headers you wish the sdk to send by default
// for example, user-agent or other analytics headers
// otherwise, return an empty object

export function getSdkHeaders(service, version, operation) {
  return {};
}
